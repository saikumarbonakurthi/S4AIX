{
  "name": "line-item-super-query",
  "version": "1.0.0",
  "private": true,
  "description": "SAP Fiori List Report app",
  "scripts": {
    "start": "ui5 serve --open index.html",
    "build": "ui5 build --clean-dest --dest dist",
    "deploy": "fiori deploy --config ui5-deploy.yaml",
    "deploy-test": "fiori deploy --config ui5-deploy.yaml --test --yes"
  },
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1.16.4",
    "@ui5/cli": "^3.11.5"
  }
}
